# ATMO RL policies

Drop the landing stage 1 policy here as:

```text
atmo_landing_stage1.pth
```

The RL nodes look here by default.  You can also point at any checkpoint or
TorchScript export with:

```bash
export ATMO_RL_POLICY_PATH=/absolute/path/to/atmo_landing_stage1.pth
```
